---
users: # namespace, only single level namespace is supported and mandatory
  db: users_db
  table_name: users
  rows:
    default: &default
      email: example@admin.com
      nickname: Martin
      age: 18
