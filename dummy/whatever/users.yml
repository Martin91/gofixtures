---
users: # namespace, only single level namespace is supported and mandatory
  db: users_db
  table_name: users
  rows:
    DEFAULT: &DEFAULT
      email: example@admin.com
      nickname: ${faker.Username()}
      age: 18
    user_age_20:
      <<: *DEFAULT
      age: 20
